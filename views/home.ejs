<%- include('partials/header') -%>

    <h1>Home</h1>
    <p>Every time you post something online, you have a choice. 
        You can either make it something that adds to the
        happiness levels in the world—or you can make it something that takes 
    </p>

    <% for(var i =0; i< blogsList.length;i++) {  %>
        <h1><%= blogsList[i].title %></h1>
        <p><%= blogsList[i].blogtext.substring(0,100) + "..." %> 
            <a href="/posts/<%=blogsList[i].title %>">Readmore</a>
        </p>
    <%}%>

<%- include('partials/footer') -%>